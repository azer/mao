Desc("syntax errors", func(it It) {

	it("should appear correctly", func(expect Expect) {
		a := 123
	})

	it("should appear correctly", func(expect Expect) {
		// b :=
	})

	it("should appear correctly", func(expect Expect) {
		c := 456
	})

})
